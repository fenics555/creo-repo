# AUDIT_20.09.2026 — соответствие: скилы / манифест v1.1 (19-20.09) / .clinerules v6.1
Исполнитель: Cline (GLM). Граница: код харвеста+окно (спеки 84/98/101) и слой правил.

## Состояние (живые цитаты)
- git tools/repo: чистые (autosave a3988ed, 1474987…8d96215).
- Агент: «0.0.0.0:8765 LISTENING 17624».
- skills_check: 13 нарушений в старых крах-скилах (см. P5).

## Находки
P1. harvest.py сломан спекой 101: удалён импорт-строка; NameError 're'
    Цитата: harvest.py:8 «import json» + :13 «RE_CREO = re.compile(...)» → NameError: name 're' is not defined.
    Окно: CLI и ночь мертвы целиком; агента не касается (harvest — свой процесс).
    Варианты: A1 вернуть строку импорта (сделано этой ногой, py_compile OK, run PROBA ожил);
    B1 откат harvest.py к 1474987 (потеря правок 101 по harvest).
    Рекомендация: A1, потому что сохраняет и 84/98, и 101.
    Проверенный: py_compile + run PROBA «START/FINISH D:\AI\PROBA» в spec98_hv_out.txt.

P2. harvest_gui.py переписан спекой 101 (HarvestGUI + panels), спека-98-версия (242 строки,
    палитра Давыдовки) — только в git 1474987
    Цитата: harvest_gui.py:10 «BG = '#ffffff'» (не #e9edf1); :7 «from harvest_gui_panels import …».
    Окно: дизайн Давыдовки не соблюдён; пути lock/report относительные (cwd-зависимость);
    settings/report перепутаны (REPORT = data\harvest_settings.json).
    Варианты: A1 оставит 101 как базу, перенести токены/абс-пути в неё (отдельная спека);
    B1 вернуть 98-версию (потеря работы 101).
    Рекомендация: A1 — панели 101 живые («GUI-101 import OK … badge='READY'»), добор токенов дешевле.
    Проверенный: проба spec101_gui_probe.py.

P3. Прогресс-файлы в двух местах
    Цитата: .clinerules «D:\AI\repo\PROGRESS_spec<NN>.md» vs практика data\progress_spec<NN>.md
    (слово пользователя в спеке 84: «дописать блок в data\progress_spec84.md»).
    Окно: следующая нога не найдёт передачу.
    Варианты: A1 менять .clinerules на data\; B1 переносить файлы в repo.
    Рекомендация: A1 — прогресс связан с data-якорями и словом пользователя.
    Проверенный: оба файла существуют живыми чтениями.

P4. tmp-файлы ноги писались в tools\data\tmp вопреки дельте «только в свою урну D:\AI\log\urn\<исполнитель>\»
    Цитата: data\tmp\spec84_extract.py, spec98_f3.py и др. (10+ файлов).
    Окно: мусор копится вне урны; log_clean.py их не чистит.
    Варианты: A1 будущие tmp — в D:\AI\log\urn\cline\; B1 ретро-перенос старых (удаление/перенос вне tmp — только словом).
    Рекомендация: A1 немедленно, B1 по слову.

P5. Старые крах-скилы не по шаблону конституции (13 шт)
    Цитата: skills_check «SKILL_crash_execution-loop.md: name mismatch (found execution-loop…)»
    и «missing ОШИБКА (grep field)» ×5, «missing executor field» ×4.
    Окно: grep-поиск по «ОШИБКА» мимо; имя/файл рассинхрон.
    Варианты: A1 отдельная нога-чистка по шаблону; B1 смягчить чекер.
    Рекомендация: A1 — конституция выше.
    Проверенный: skills_check вывод живой (data\tmp\skills_check_out.txt).

P6. Гипотеза «purge не учитывает доступность корней» — ОПРОВЕРГНУТА
    Цитата: harvest.py «vanished_guard» + лог «purge пропущен: спящие корни» (контракт спеки 84).
    Вычеркнуть из заставки гипотез при следующем аудите.

## Долги трёх рук (манифест 19/20.09)
harvest: CLI ✓, окно ✓ (101), визард в витрине под щитом — НЕТ (строка долга: недостающая рука — визард).

СТОП, жду решения пользователя.
