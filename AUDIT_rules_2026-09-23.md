# АУДИТ ПРАВИЛ ДОМА — AUDIT_rules_2026-09-23.md

Исполнитель: Cline (облачная нога). Граница: уровни 1–4 (репо, карта, паспорт, иерархия правил),
уровни 5–6 — частично. Дата: 23.09.2026. Протокол: `SKILL_audit_protocol.md`.

## 0. ЦИТАТА СОСТОЯНИЯ (п.9.2)

- `git -C D:\AI\repo status --short` → пусто (рабочее дерево чистое).
- Последний коммит: `15c5053 2026-09-23` (скилл природы модели, карта Давыдовки).
- `dir D:\AI\repo\AUDIT_*.md` → **0 файлов** — предыдущих отчётов аудита в репо НЕТ,
  история (п.1.2 протокола) начинается этим отчётом.
- Корень репо: 19 `.md`-файлов + `.clinerules`, `.gitignore`, `GIT_SYNC_REPO.bat`, `LICENSE`.
- `crash\`: 43 файла `.md` (конституция + индекс + 41 экземпляр).
- `D:\AI\repo\.clinerules` → Length 0 (23.09.2026 11:56:51), отслеживается гитом;
  живой адаптер `D:\AI\.clinerules` → 22 995 байт, v6.3 (22.09.2026 22:43:10).

## УРОВЕНЬ 1: РЕПО И КАРТА

### P1. Пустой `.clinerules` в корне репо — ловушка для следующего README
Цитата: `README.md:21` — «Copy `.clinerules` to your agent root (default `D:\AI`) — it loads
automatically on session start.» Факт: файл в репо — 0 байт; живой адаптер — 22 995 байт в `D:\AI\`.
Окно: следующий README-читатель скопирует 0 байт поверх живого адаптера → на старте задачи
исчезнут правила транспорта, анти-петли и строка-доказательство. Заметит по первому краху сессии.
Варианты: A1 — подшить копию живого адаптера в репо; B1 — удалить пустышку по слову пользователя
и поправить README на абсолютный путь (спека после решения); C1 — не трогать.
Сценарии:
| Вариант | Кто | Что меняется | Рестарт | Цена | Худший случай / откат |
|---|---|---|---|---|---|
| A1 | нога + редактор | 1 файл в репо | нет | 2 мин | расхождение копий при следующей правке → лечится правилом «источник = D:\AI\.clinerules» |
| B1 | нога | −1 файл + README по спеке | нет | 5 мин | читатель без файла → лечится правкой README в той же спеке |
| C1 | — | 0 | нет | 0 | сохраняется риск уничтожения живого адаптера |
Рекомендация: **A1** — копия в репо гасит риск уничтожения живого адаптера и выполняет обещание
README; источник правды остаётся `D:\AI\.clinerules`.
Пометка «проверенный»: `Get-Item`/`git ls-files` цитатой выше.

### P2. `SKILL_index.md` ссылается на `AUDIT_rules_*.md` — файлов истории нет
Цитата: `SKILL_index.md:84` — «**Аудит**: `AUDIT_rules_*.md` и `SKILL_audit_protocol.md`».
Факт: `Get-ChildItem D:\AI\repo -Filter 'AUDIT*' -Recurse` → 0; в `D:\AI\` и `D:\AI\log\reports\`
тоже 0. Протокол (п.1.2) требует начинать с diffs предыдущих аудитов — diffs не с чего снять.
Окно: закрытые находки теряются, лечение повторяется; заметит следующий аудитор на шаге 3.
Варианты: A2 — считать этот отчётом началом истории, карту не менять (паттерн совпадает);
B2 — дописать в карту «история: с 23.09.2026» (правка = спека после решения).
Сценарии: A2 — 0 мин, риск нулевой; B2 — 2 мин, но функции не добавляет.
Рекомендация: **A2** — имя файла уже по паттерну карты.
Пометка «проверенный»: `Get-ChildItem 'AUDIT*'` → пусто (цитата выше).

### P3. Индекс крахов врёт по счётчику и имеет две сироты
Цитата: `crash\SKILL_crash_index.md:54` — «Всего крах-скиллов: 40 (без учёта конституции и этого
списка).» Факт: на диске 41 экземпляр без конституции и списка; в индексе НЕ упомянуты
`SKILL_crash_git_commit-sweeps-foreign-staged.md` и `SKILL_crash_git_parallel-leg-rebase-reverts-worktree.md`.
Окно: две свежие git-подписи недоступны навигации; счётчик врёт на 1. Вред ограничен навигацией;
заметит ищущий «rebase» или «git» в индексе.
Варианты: A3 — дописать две строки и поправить число на 41 (правка = спека после решения);
B3 — завести долг в разделе «ДОЛГИ И ИДЕИ» карты, файлы не трогать.
Сценарии: A3 — 3 мин, риск = опечатка в описании (лечится чтением); B3 — 1 мин, сироты остаются
невидимыми.
Рекомендация: **A3** — индекс это навигация, две строки закрывают её полностью.
Пометка «проверенный»: сверка списка файлов против текста индекса скриптом (цитата выше).

### P4. `AGENT_MAP.md` — сирота: файл есть, карта не знает
Цитата: `Select-String -Pattern 'AGENT_MAP'` по `SKILL_index.md`, `README.md`, `MANIFEST.md`,
`PASSPORT.md` → **0 совпадений**. Файл: `AGENT_MAP.md`, 23.09.2026, priority high — карта
строения агента (43 блока / 143 инструмента, порт 8765, риски двойных сессий Creo).
Окно: карта агента невидима из карты скиллов; задача «чини агента» ведёт мимо. Заметит при
первой задаче на `D:\AI\tools\agent`.
Варианты: A4 — строка в `SKILL_index.md` (Core/Программирование); B4 — включить в входы
направления MANIFEST (избыточно: файл high, не critical).
Сценарии: A4 — 2 мин, риск нулевой; B4 — 5 мин, раздувает закон ради навигации.
Рекомендация: **A4** — карта скиллов и есть реестр указателей.
Пометка «проверенный»: `Select-String` по четырём файлам → 0 (цитата выше).

## УРОВЕНЬ 2: ПАСПОРТ И ВВОДНЫЕ

### P5. `PASSPORT.md` называет несуществующий модуль и отстаёт по датам
Цитата: `PASSPORT.md:24` — «| `sched.py` | ночной цикл и сторож |». Факт:
`Test-Path 'D:\AI\tools\agent\sched.py'` → **False**; `agent_sched.py` → **True**.
Цитата: `PASSPORT.md:49` — «*Обновлено: 21.09.2026*» против `AGENT_MAP.md:8` — «date: 23.09.2026»
и продвинутой уборки 23.09 в секциях агента.
Окно: исполнитель ищет `sched.py` и не находит; дата занижает доверие к остальным строкам.
Заметит при задаче «ночной цикл/сторож».
Варианты: A5 — правка одной строки + даты (спека после решения); B5 — не править руками,
восстановить ближайшим аудитом входных.
Сценарии: A5 — 2 мин, риск = забыть параллельные упоминания (лечится grep); B5 — 0 мин сейчас,
ложная строка живёт дольше.
Рекомендация: **A5** — имя модуля это легко проверяемый факт, паспорт обязан совпадать с диском
(п.3 протокола).
Пометка «проверенный»: `Test-Path` обеих строк цитатой выше.

### P6. `README.md` обещает `tools/agent/` внутри репо — папки нет
Цитата: `README.md:17` — «| `tools/agent/` | the agent itself (see its own README) |». Факт:
`Test-Path 'D:\AI\repo\tools'` → **False**; агент живёт в `D:\AI\tools\agent`.
Окно: читатель при первом клоне ищет агента в репо и не находит.
Варианты: A6 — абсолютный путь в таблице; B6 — оставить (контекст D:\AI читается из строки 21).
Сценарии: A6 — 1 мин, риск нулевой; B6 — 0 мин, строка остаётся ложной для клона на другой машине.
Рекомендация: **A6** — абсолютный путь и так используется в README (строки 21, 46).
Пометка «проверенный»: `Test-Path` цитатой выше.

## УРОВЕНЬ 3: ПРАВИЛА И ИЕРАРХИЯ

### P7. Живой адаптер ведёт на несуществующие пути Creo-скиллов
Цитата: `D:\AI\.clinerules:27-29` — «Creo\SKILL_creoson_complete.md,
Creo\SKILL_creoson_sessions_workdirs.md, Creo\SKILL_creoson_rename_mechanism.md,
Creo\SKILL_creoson_write_rules.md, Creo\SKILL_creoson_probe_method.md.»
Факт: `Test-Path` всех пяти в `D:\AI\repo\Creo\` → **False**; файлы лежат в
`D:\AI\repo\Creo\CREOSON\` (проверено: `Creo\CREOSON\SKILL_creoson_complete.md` → True).
Строка 174 (`Creo\SKILL_creo_index.md`) жива.
Окно: п.3 старта задачи адаптера требует читать эти файлы ЦЕЛИКОМ ДО первого вызова; чтение
умирает → нарушение строки-доказательства или трата попыток на поиск. Цепочка 4.1 протокола
сломана в автозагружаемом слое. Заметит на каждой CREOSON-задаче.
Варианты: A7 — исправить префикс на `Creo\CREOSON\` в пяти строках; A7-альт — свести на
указатель `Creo\SKILL_creo_index.md`; B7 — не трогать (читатель пойдёт по индексу интуитивно).
Сценарии:
| Вариант | Кто | Что меняется | Рестарт | Цена | Худший случай / откат |
|---|---|---|---|---|---|
| A7 | нога + редактор, 5 строк адаптера | дельта `D:\AI\.clinerules` | нет (слой читается на старте задачи) | 5 мин | опечатка пути → та же поломка; откат = копия `pre_` |
| A7-альт | 1 строка | дельта адаптера | нет | 3 мин | маршрут через индекс +1 read на старте |
| B7 | — | 0 | — | 0 | поломка живёт: нарушения старта на каждой CREOSON-задаче |
Рекомендация: **A7** — прямые имена дают меньше ходов на старте, чем маршрут через индекс.
Пометка «проверенный»: `Select-String -Pattern 'Creo\\SKILL_'` (строки 27–29, 174) + `Test-Path`
шести путей — цитаты выше.

### P8. Манифест: версия не отражает п.19–20; приложение аудит-протокола не вычёркивает опровергнутые гипотезы
Цитата: `MANIFEST.md:2` — «Версия 1.1, 18.09.2026…», при этом файл содержит п.19 (20.09.2026)
и п.20 (21.09.2026). Цитата: `SKILL_audit_protocol.md` (приложение) — «манифест кончается пунктом 15»
(опровергнуто: манифест доходит до п.20); «конституция без раздела ВОСКРЕШЕНИЕ» (опровергнуто:
`crash\SKILL_crash_constitution.md:44` — «== ВОСКРЕШЕНИЕ И ДИАГНОСТИКА ПОСЛЕ ОБМОРОКА ==»).
Протокол сам требует вычёркивания опровергнутых гипот с цитатой и именем аудита — не вычеркнута ни одна.
Окно: устаревшая шапка закона и ложные гипотезы у следующего аудитора; искажает доверие к датам,
не ломает работу напрямую.
Варианты: A8 — поднять манифест до «Версия 1.2, 23.09.2026» + вычертить гипотезы (спека после
решения); B8 — только вычертить гипотезы (правило само предписывает); B8-альт — не трогать.
Сценарии: A8 — 10 мин, 2 файла, версия закона требует слова пользователя; B8 — 5 мин, 1 файл,
правило выполняется без смены закона; B8-альт — 0 мин, правило остаётся невыполненным.
Рекомендация: **B8** — вычёркивание прямо предписано протоколом и не меняет закон; подъём версии
манифеста отложить до слова пользователя.
Пометка «проверенный»: чтение шапки манифеста, строк 40/46/50 и строки 44 конституции цитатой выше.

## УРОВЕНЬ 5–6 (частично, честно)

- Воскрешение: триггеры в автозагружаемом слое (`D:\AI\.clinerules`, п.2 — перечитывание скилла
  выживания на REVIVE) — цепочка 4.3 жива. **Подтверждено.**
- Указатели `Prog\SKILL_agent_protocol.md` (1 592 б) и `Prog\SKILL_local_agent_cline.md` (162 б) —
  однострочные отсылки к корню («дублирование закона запрещено»): Золотой стандарт, п.7 —
  **нарушений нет.**
- Долги карты (`SKILL_tool_template`, `SKILL_architect_reviewer`, `SKILL_log_management`,
  `SKILL_skill_craft`) помечены «долг: файла нет», файлов действительно нет — **честность соблюдена.**
- Устаревшие долги карты: `SKILL_index.md:120` «PDF: домен пуст» — в `PDF\` уже три файла
  (`SKILL_pdf_index`, `SKILL_pdf_control`, `SKILL_pdf_routine`); `:122` «Web: в папке один сырой
  файл» — в `Web\` уже `SKILL_web_index.md`. **Расхождение внутри карты** — правка по спеке.
- Гейты кода, щит согласования, ротация бэкапов — **не проверялись** (вне рамки уровней 1–4),
  пометка «не подтверждено».

## СВОДНАЯ ТАБЛИЦА ПРОВЕРЕННЫХ ВАРИАНТОВ (по приоритетам)

| № | Находка | Приоритет | Рекомендация | Цена | Путь пробы |
|---|---|---|---|---|---|
| P7 | Битые пути Creo-скиллов в живом адаптере | **высший** — ломает старт CREOSON-задач | A7: `Creo\CREOSON\` в 5 строках | 5 мин + копия `pre_` | повторный `Test-Path` всех 5 после правки |
| P1 | Пустой `.clinerules` в репо под инструкцией README | **высший** — риск уничтожения живого адаптера | A1: подшить копию живого | 2 мин | `Get-Item Length` = 22995 после правки |
| P3 | Индекс крахов: 40 ≠ 41, две сироты | средний | A3: две строки + число 41 | 3 мин | повторная сверка списков скриптом |
| P4 | `AGENT_MAP.md` — сирота карты | средний | A4: строка в `SKILL_index.md` | 2 мин | `Select-String AGENT_MAP` → 1 |
| P5 | `PASSPORT.md`: `sched.py` не существует | средний | A5: `agent_sched.py` + дата | 2 мин | `Test-Path` после правки |
| P8 | Версия манифеста 1.1/18.09 при п.19–20; гипотезы не вычеркнуты | низкий | B8: вычертить гипотезы | 5 мин | чтение приложения после правки |
| P6 | README: `tools/agent/` не существует в репо | низкий | A6: абсолютный путь | 1 мин | `Test-Path` |
| P2 | `AUDIT_rules_*.md` в карте без файлов истории | низкий (закрыт этим отчётом) | A2: ничего не менять | 0 | имя файла уже по паттерну |

СТОП, жду решения пользователя.

