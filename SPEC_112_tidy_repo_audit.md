# SPEC 112 «Причёска репо по аудиту напарника 22.09»
Источник: сообщение пользователя 22.09.2026 («от друга подсказка»). Текст ниже — verbatim.
Принята ногой 1 (Cline) 22.09.2026. Флаги расхождения спеки с диском — PROGRESS_spec112.md (Ф0)
и D:\AI\log\reports\REPORT_spec112_cline_20260922.md. Сама спека не правится (источник чужой).

--- verbatim begin ---
СПЕКА 112 (причёска репо по аудиту напарника 22.09): источники правки = диск и цитаты
Ф0, никогда окно; записей на Z: нет; рука пользователя только в приёмке.
Ф0. ЦИТАТЫ: Length семи файлов (корень и Prog скилла выживания, корень и Prog
протокола, crash и Prog editor_too_large, Prog unit_testing); Select-String счётом:
'пять точек' по корню и Prog-копии, 'этот файл — указатель' по каждому из шести,
заголовок unit_testing по своему файлу; Test-Path D:\AI\repo\agents и
D:\AI\repo\Creo\SKILL_creo_index.md; Get-ChildItem D:\AI\repo\Creo\*.md и
D:\AI\repo\agents -Recurse -Filter SKILL_*.md именами; git log --oneline -2.
Ф1. ОДИН ЭКЗЕМПЛЯР: корневой SKILL_local_agent_cline.md = yaml-шапка из текущего
корня (строки до первого заголовка) плюс тело из Prog-копии МИНУС строка-указатель и
ведущие пустые (тело не правится ни символом); Prog-копия = одна строка-указатель;
Prog\SKILL_agent_protocol.md = одна строка-указатель; Prog\SKILL_editor_too_large.md =
одна строка-указатель на crash\ (crash-копию не трогать, если в ней нет строки-
указателя; если есть — снять её там); Prog\SKILL_unit_testing.md = первая копия до
второго заголовка; цитаты после: 'пять точек' корень=1, указателей по одному в трёх
Prog-файлах и ноль в корнях и crash, заголовков unit=1, Length Prog-копий < 300.
Ф2. ЧЕСТНАЯ КАРТА (якорные правки SKILL_index.md): секцию 6 заменить вербатим-блоком
регистрации agents, ЕСЛИ Ф0 показала файлы: «### 6. Agents (Автономные исполнители) —
файлы есть, происхождение внешнее; подгружаются по надобности (priority normal):
задача о RAG, аудите безопасности, коде без галлюцинаций; до первой успешной пробы
цитировать как внешний источник, не закон дома» плюс три строки с путями из Ф0; если
файлов нет — оставить строку идей и добавить «проверено Test-Path <дата>»; шапку
секции 2 заменить на «вход направления = эта секция (решение 22.09: единственная
прописка)», строку долга Prog-индекса в секции 7 на «закрыт решением 22.09: вход =
секция 2, индекс не создаётся»; строку object_creoson дополнить префиксом Creo\ и
Move-Item файла из корня в Creo\; цитаты строк после.
Ф3. ТОЧКА ВХОДА CREO: создать Creo\SKILL_creo_index.md одним write_file: фронтматтер
(name creo-index, system creo, priority critical, when: creo, направление, вход),
список из секции 1 карты дословно, каждая строка сверена с цитатой папки Ф0; файл из
карты отсутствует на диске = строка «долг: файла нет» вместо молчания; последняя
строка «вход направления, состав сверен с папкой <дата>»; цитата: Test-Path True,
голова 6 строк.
Ф4. ВЫЗЫВАЕМОСТЬ ПРОБОЙ: для каждого critical-скилла каждого направления (Creo пять,
Prog три, Core три) прочитать голову 3 строки и процитировать таблицу «скилл → путь →
When есть/нет → priority»; нет When или файла = строка долга в отчёт, не молчание;
Prog-скиллы с текстовым паспортом вместо yaml-фронтматтера = наблюдение строкой
«паспорт текстом принят, унификация не требуется», не правка.
Ф5. БУМАГИ И РЕГИСТРАЦИЯ: PROGRESS_spec112.md блоками перед «=== END ===»; отчёт-файл
REPORT_spec112_<исполнитель>_<дата>.md с таблицей аудита «находка → вердикт →
цитата»; новый крах-скилл crash_pointer-with-body: ОШИБКА дословно «строка-указатель
вставлена СВЕРХ тела вместо замены тела: закон живёт в двух местах под табличкой
указателя»; ПРОФИЛАКТИКА: указатель = ВСЁ содержимое файла одним write_file, приёмка
Length < 300 байт цитатой; ПОВТОРЫ 3 (agent_protocol, editor_too_large,
local_agent_cline, все 21-22.09); GIT_SYNC_REPO сразу после Ф1 и после Ф2 отдельно.
ГРАБЛИ: тело закона 21.09 не правится ни символом (перенос как есть); editor по файлам
свыше 3 КБ запрещён, перезаписи = write_file; два отказа = смена метода со строкой
«ПОПЫТКА N:»; нарратив без вызова инструмента = нарушение, WHY-строка одна на ход.
--- verbatim end ---