# АУДИТ ДОМА: ДОСТУПНОСТЬ СКИЛЛОВ И ПРАВИЛА — AUDIT_rules_2026-09-26.md

Исполнитель: Cline (локальная нога, режим act). Граница: уровни 1-4 протокола
(`SKILL_audit_protocol.md`) — доступность скиллов целиком, правила дома целиком;
уровни 5-6 — частично. Дата: 26.09.2026.

## 0. ЦИТАТА СОСТОЯНИЯ
- `git -C D:\AI\repo status --porcelain` → пусто (чисто).
- `git -C D:\AI status --porcelain` → ` M .clinerules` (единственная строка).
- Копии адаптера: `D:\AI\.clinerules` и `D:\AI\repo\.clinerules` — оба 25 142 б,
  sha8=36023d4d (SHA256 36023D4D…1788E), mtime 2026-09-24 16:08 — **совпадают**.
- Предыдущий аудит: только `AUDIT_rules_2026-09-23.md` (18 199 б).

## 1. МЕТОД (живые цитаты, не память)
1. Штатный `D:\AI\tools\agent\dev\skills_check.py` → `violations=0 notes=17`.
2. Свой read-only линкчекер `D:\AI\log\urn\cline\skills_audit.py` (урна исполнителя):
   132 файла-скилла, 0 сирот, 0 пустых, 7 ghost-ссылок, 12 дублей basename,
   27 замечаний к шапкам. Полный отчёт: `D:\AI\log\urn\cline\skills_audit_out.txt`.
3. Свой линкчекер путей `D:\AI\log\urn\cline\paths_check.py` по 16 файлам правил
   (адаптер, манифест, карта, паспорт, входы направлений). Отчёт:
   `D:\AI\log\urn\cline\paths_check_out.txt`.
4. `Get-FileHash`, `Get-ChildItem -Recurse -Include`, `Select-String`, `git status`.

## 2. ЧИСТО (проверено, не находка)
- 13 обязательных файлов правил: missing=0 (манифест 9 821 б sha8=142818f8;
  скилл выживания 21 573 б sha8=db1a8391; карта 16 242 б; протокол аудита 15 156 б;
  паспорт 3 858 б; AGENT_MAP 22 814 б; CULTURE_files 9 459 б; конституция краха
  5 623 б; индекс крахов 10 719 б).
- Входы всех семи направлений на диске: `Creo\SKILL_creo_index.md`,
  `PDF\SKILL_pdf_index.md`, `Web\SKILL_web_index.md`,
  `Инженерные\SKILL_engineering_index.md`, `Трейлы\SKILL_trails_index.md`,
  `Ошибки\SKILL_errors.md` (2 510 б), `crash\SKILL_crash_constitution.md`.
- **P7 прошлого аудита закрыт**: все пять Creo-скиллов из живого адаптера на месте
  (`Creo\CREOSON\SKILL_creoson_complete.md` 13 276 б, `…_sessions_workdirs.md`
  22 575 б, `…_rename_mechanism.md` 4 970 б, `…_write_rules.md` 3 385 б,
  `…_probe_method.md` 4 227 б). Ложная тревога `SKILL_creo_commands.md`
  отклонена: файл есть в `Creo\CREOSON\` (3 777 б).
- **P1 прошлого аудита закрыт**: `.clinerules` в repo больше не пустышка (25 142 б).
- **P3 прошлого аудита закрыт**: `crash\` = 42 `SKILL_*.md` + 4 `crash_*.md`;
  без конституции и списка экземпляров ровно 44 — совпадает со строкой индекса
  «Всего крах-скиллов: 44»; две git-сироты (строки 54-55 индекса) внесены.
- 132 файла-скилла по папкам: Creo 47, crash 42, Prog 14, root 10, Инженерные 5,
  data 5, PDF 3, agents 3, Web 1, Ошибки 1, Трейлы 1. Сирот нет: каждое имя
  упомянуто хотя бы в одном носителе карты.
- Обещания паспорта живы: `D:\AI\tools\STOP_ALL.ps1` есть; `Creo\CREO_MAP.md` есть
  (но путь в AGENT_MAP врёт — см. R4).

## 3. НАХОДКИ

### R1. Незакоммиченная правка живого адаптера в корневом мини-репо
Цитата: `git -C D:\AI status --porcelain` → ` M .clinerules`.
Окно: следующий `git checkout/restore` в корневом репо откатит правила транспорта,
анти-петли и строку-доказательство — нога следующей задачи стартует по старому
закону; заметит это только по краху сессии. Копии на диске при этом синхронны.
Варианты: A — `GIT_SYNC_ALL.bat` (штатный канал дома); B — адресно
`git add .clinerules` + `git commit -m "…" -- .clinerules`; C — не трогать.
Сценарии: A — 1 мин, коммит обеих копий, откат = `git revert`; B — 1 мин, точнее
по составу, откат = `git revert`; C — 0 мин, риск отката живёт.
Рекомендация: A (штатный путь дома), при параллельных ногах — B.
Проверенный: `git status --porcelain` до/после (пусто = успех).

### R2. Карта ошибок знает 2 файла из 4 (сироты в `Ошибки\`)
Цитата: `Ошибки\SKILL_errors.md` — правило «Каждая разобранная ошибка = файл
`ERR_ГГММДД_имя.md` + строка в карте ниже», таблица «Карта» содержит 2 строки
(ERR_260819_stdlib_code.md, ERR_creoson_write_ops.md).
Факт: на диске 4 файла — сироты `Ошибки\ERR_260902_web_fetch.md` (737 б) и
`Ошибки\ERR_creoson.md` (13 712 б).
Окно: разобранные ошибки не находятся по карте — лечение изобретается заново;
заметит ищущий ERR/Traceback.
Варианты: A — дописать 2 строки в таблицу карты (спека после слова); B — свести
содержимое сирот в строку; C — не трогать.
Сценарии: A — 5 мин, 1 файл, без рестарта, откат = git revert; B — дольше, риск
потери контекста; C — 0 мин, правило файла остаётся нарушенным.
Рекомендация: A. Проверенный: чтение карты после правки, 4 строки.

### R3. Ghost-ссылки карты: незавершённое переименование и закрытый долг
Цитата: `SKILL_index.md` — `Prog\SKILL_calc_script_architecture.md` (нет файла;
тело живёт как `STANDARD Engineering Calculation Script Architecture.md`, 15 847 б);
`Prog\SKILL_prog_index.md` (нет файла, решение 22.09 — «индекс не создаётся»).
Окно: ссылка на несуществующий путь; ищущий решит, что скилл потерян.
Варианты: A — выполнить переименование по слову пользователя и убрать строку про
prog_index; B — заменить ссылку на текущее имя файла; C — не трогать.
Рекомендация: B (минимально и без риска потери тела), переименование — отдельной
спекой по слову пользователя. Проверенный: `Test-Path` обеих целей до/после.

### R4. AGENT_MAP: два пути-призрака
Цитата: `AGENT_MAP.md` → `D:\AI\repo\CREO_MAP.md` (фактически `Creo\CREO_MAP.md`)
и `D:\AI\PROBA\agent_tools_inventory.txt` (файла нет ни в `D:\AI\PROBA`, ни в repo).
Окно: реестр инструментов/карт шлёт читателя в пустоту.
Варианты: A — исправить путь CREO_MAP и снять/переписать строку inventory;
B — пометить «не подтверждено»; C — не трогать. Рекомендация: A.
Проверенный: `Test-Path 'D:\AI\repo\Creo\CREO_MAP.md'` = True после правки.

### R5. Два одноимённых тела: `liteika_hts_mm_relations.md`
Цитата: `Creo\cards\liteika_hts_mm_relations.md` (67 130 б, 16.09.2026) и
`Creo\LIT\liteika_hts_mm_relations.md` (77 218 б, 24.09.2026).
Окно: Золотой стандарт п.7 «два одноимённых тела — нарушение»; читатель возьмёт
устаревшую версию из cards.
Варианты: A — сверить diff и оставить одно тело + указатель; B — переименовать
карточку в cards по её смыслу; C — не трогать. Рекомендация: A после diff (нужно
слово пользователя: удаление вне tmp/backup запрещено). Проверенный: diff + один файл.

### R6. Индекс PDF лжёт о содержимом папки
Цитата: `PDF\SKILL_pdf_index.md:6` — «ЧТО ЗДЕСЬ: (папка пуста)».
Факт: в `PDF\` три файла — `SKILL_pdf_index.md`, `SKILL_pdf_control.md`,
`SKILL_pdf_routine.md` (это вход направления!).
Окно: нога, открыв вход PDF, решит, что домен мёртв; заметит ищущий скилл контроля PDF.
Варианты: A — переписать шапку индекса по факту; B — удалить строку; C — не трогать.
Рекомендация: A. Проверенный: чтение индекса после правки.

### R7. Слепая зона проверки имён: `Creo\STANDARDS`
Цитата: `SKILL_parameters_guide.md` — `name: parameters-mp` при файле
`SKILL_parameters_guide.md`; `SKILL_reference_limits.md` — `name: creo-limits`.
Окно: штатный `skills_check.py` проверяет только `crash\` и корень/Prog — эти два
нарушения канона имён не видит никто.
Варианты: A — привести `name:` к имени файла; B — расширить `skills_check.py` на
все папки (правка кода дома); C — не трогать. Рекомендация: A (дешевле); B — отдельной
спекой. Проверенный: повторный прогон своего линкчера → 0 замечаний в STANDARDS.

### R8. Внешние агенты в `agents\`: `name:` в кавычках
Цитата: `agents\rag-architect\SKILL_rag_architect.md` → `name: "rag-architect"`;
`agents\skill-security-auditor\SKILL_skill_security_auditor.md` → `name: "skill-security-auditor"`.
Окно: формальное несоответствие канону; на работу дома не влияет (внешние агенты).
Варианты: A — снять кавычки; B — не трогать (дом не владеет форматом внешних агентов).
Рекомендация: B. Проверенный: чтение шапок.

### R9. Семь ghost-ссылок = пять долгов + переименование + Избранное
Цитата: ссылки без файлов: `SKILL_architect_reviewer`, `SKILL_log_management`,
`SKILL_skill_craft`, `SKILL_parallel_local_leg`, `SKILL_prog_index` — карта сама
помечает «долг: файла нет»; `SKILL_calc_script_architecture` — R3;
`SKILL_favorites_` — долг карты («Избранное/»).
Окно: честность соблюдена (карта говорит «нет файла»), вреда нет.
Рекомендация: оставить, долги вести в разделе «ДОЛГИ И ИДЕИ» карты.

### R10. Дубли basename (12) — разобраны, нарушения нет
8× `_INDEX.md` (по индексу в каждой подпапке Creo — норма), 4× в
`data\backup\pre_lang_fix\` (бэкапы — норма), `STUDY_NOTES.md` (5 тем в `ИЗУЧЕНО` —
норма), указатели `Prog\SKILL_local_agent_cline.md` и `Prog\SKILL_editor_too_large.md`
(одна строка «закон живёт…», норма п.7 Золотого стандарта), `readme.md` — норма.
Единственный настоящий дубль-темы — R5.

## 4. СВОДНАЯ ТАБЛИЦА (по приоритетам)
| № | Находка | Приоритет | Рекомендация | Цена | Путь пробы |
|---|---|---|---|---|---|
| R1 | `M .clinerules` в корневом репо | средний | A: GIT_SYNC_ALL.bat | 1 мин | `git status --porcelain` → пусто |
| R2 | 2 ERR-сироты вне карты ошибок | средний | A: 2 строки в карту | 5 мин | чтение карты, 4 строки |
| R6 | `PDF\SKILL_pdf_index.md` «папка пуста» | средний | A: шапка по факту | 3 мин | чтение индекса |
| R3 | Ghost `calc_script_architecture`, `prog_index` | низкий | B: ссылка на живое имя | 3 мин | `Test-Path` |
| R4 | AGENT_MAP: 2 пути-призрака | низкий | A: исправить/снять | 3 мин | `Test-Path` |
| R5 | Дубль-тело `liteika…` | низкий | A: diff + один файл | 15 мин | diff перед удалением |
| R7 | `name:` ≠ файл в `Creo\STANDARDS` | низкий | A: привести шапки | 3 мин | повторный линкчекер |
| R8 | `agents\`: `name:` в кавычках | низкий | B: не трогать | 0 | — |
| R9 | 7 ghost = заявленные долги | низкий | оставить | 0 | карта «ДОЛГИ» |

## 5. ЧЕСТНО (вне рамки)
- Уровни 5-6 (код против правил, гейты токенов, щит согласования, сторожа, ротация
  бэкапов) — **не проверялись**, пометка «не подтверждено».
- Уровень 2 (паспорт) проверен частично: пути паспорта живы, полнота состава против
  живых портов и процессов не сверялась.

## 6. ЛЕЧЕНИЕ (по слову пользователя 26.09.2026: «да исправляй всё»)
| № | Что сделано | Проверка |
|---|---|---|
| R2 | `Ошибки\SKILL_errors.md`: в таблицу «Карта» добавлены 2 строки (`ERR_260902_web_fetch.md`, `ERR_creoson.md`) | чтение карты: 4 строки ERR |
| R6 | `PDF\SKILL_pdf_index.md`: шапка переписана по факту — три файла направления, теги, порядок работы | чтение индекса |
| R3 | переименование выполнено (`git mv`): `STANDARD Engineering Calculation Script Architecture.md` → `Prog\SKILL_calc_script_architecture.md`; ссылка в `SKILL_index.md:58` обновлена, `name:` внутри приведён к `calc_script_architecture`; долг в §7 закрыт; опись «Индекс Prog» переформулирована (ложная ссылка снята) | `git status` — `RM`; `Test-Path` = True |
| R4 | `AGENT_MAP.md`: `repo\CREO_MAP.md` → `repo\Creo\CREO_MAP.md`; строка про `agent_tools_inventory.txt` переписана (файла нет — подтверждено), §3 и §10 сверены | `Test-Path 'Creo\CREO_MAP.md'` = True |
| R7 | `Creo\STANDARDS`: `name: parameters-mp` → `parameters_guide`; `name: creo-limits` → `reference_limits` (иных упоминаний в repo нет) | повторный линкчекер |
| R5 | дубли `liteika_hts_mm_relations.md` — найдено **четыре** копии (сверх аудита: `Creo\LIT\..._BEFORE.md`, `..._OLD.md`); проба `Compare-Object` дала `only_in_this=0` у всех трёх кандидатов (полные подмножества LIT, 1 329 ⊂ 1 511 строк); бекапы `repo\data\backup\pre_liteika_{cards,before,old}_2026-09-26.bak` по 67 130 б; копии сняты `git rm`; ссылка `Creo\SKILLS_MAP.md:66` переведена на живой `LIT\liteika_hts_mm_relations.md` | бекапы на месте; `dups 12 → 11` |
| R1 | `.clinerules` корневого репо закоммичен адресно, коммит `49ac90d` (у корня remote нет — push не требуется) | `git status --porcelain` корня пуст |
| R8 | НЕ правил: `agents\` — внешние агенты, формат не дома; кавычки в `name:` безвредны | — |
| R9 | НЕ правил: ghost-ссылки = заявленные долги без тел (карта честно помечает «файла нет») | — |

### Приёмка после лечения (26.09.2026, живые прогоны)
- `skills_check.py` → `violations=0 notes=17` (как до лечения; слепая зона — R7 — закрыта
  моим линкчекером: `name issues 27 → 25`).
- Мой линкчекер доступности → `required=13 missing=0`, `skill_files=133` (вошёл
  переименованный), `ghosts 7 → 6`, `dups 12 → 11`, `name_issues 27 → 25`,
  `orphans=0`, `empty=0`.
- Линкчекер путей → 214 «unresolved», из них реально битых 0: остальное — «голые имена»
  без пути (`secrets.json`, `creo_pdf_misplaced.py` и т. п.) и намеренно мёртвая строка
  `agent_tools_inventory.txt` (в тексте прямо помечена «отсутствует»).
- Остаток по слову пользователя не требовался: `SKILL_CHARGE_START.md` (автосборник
  `skills_charge.py`) несёт старое имя файла — руками автогенерируемое не правится.

СТОП, лечение исполнено по слову пользователя; приёмка зелёная.

