# SPEC 113 «Чертежи-сироты: список по дереву Work»
Источник: находки первой ноги 23.09.2026 при разборе 52 ошибок PDF-рутины (проба на живом Creo).
Исполнитель: параллельная локальная нога. Приёмка: первая нога (Cline). Спека не правится.

## НАЧАЛЬНЫЕ ДАННЫЕ (читать первым)
1. **Кто и где.** Ты — параллельная локальная нога (Ollama `gemma4:26b-131k`, окно 131k) с теми же
   правилами и скиллами, что у первой ноги. Приёмку делает первая нога (Cline).
2. **Дом.** Репо `D:\AI\repo` (скиллы, карты, спеки, индексы); агент `D:\AI\tools\agent` (тело и
   программы); логи `D:\AI\log\...`; рабочее дерево `Z:\PTC\Work` — **только чтение**; конфиги Creo
   `Z:\PTC\CREO-START\START-STD` (там боевой `config.pro`); базы `D:\AI\tools\agent\data\agent.sqlite`
   (после чистки 23.09 — 0,19 ГБ) и `harvest.db` (63 МБ).
3. **Что уже сделано по теме.** PDF-рутина дома работает (сканирует 11 901 чертёж, создаёт и обновляет
   PDF). Последний прогон дал **52 ошибки**; первая нога разобрала их на три причины: (а) **чертёж-сирота**
   — модели с тем же именем нет; (б) обрыв связи с Creo (`XToolkitCommError`); (в) попытка открыть файл
   с версией (`X.drw.1`) — Creo такие имена не принимает. Проба на живом Creo подтвердила: **чертёж без
   модели Creo не открывает** (`XToolkitNotFound`). Твоя работа — отделить сирот от настоящих ошибок.
4. **Что есть в наличии** (не изобретай заново):
   - `D:\AI\tools\agent\cmnm_scan\cmnm_scan.py` — разбор заголовков Creo-файлов (поле `#- CMNM`), уже
     учитывает служебный префикс сессии (`приваи.drw.1` → `010приваи.drw`);
   - `D:\AI\tools\agent\dup_scan\` — образец программы класса Р (папка + `*.bat` + README + лог + приёмка);
   - `agent.sqlite`, таблица `models` = **44 154** строки (name, ext, path) — инвентарь моделей;
   - `harvest.db`, `models_raw` = **93 148** строк (name, ext, path, mtime, size, sha256head), `pairs` = 24 607;
   - `Z:\PTC\Work\search.pro` — **4099** папок (пути поиска Creo), файл существует;
   - три контрольных случая для приёмки: `приваи` — сирота; `ыва` — не сирота (рядом `ыва.asm.1`);
     `00-06` в папке КТП — сирота (там только `*.drw.1` и `*.dxf`).
5. **Что прочитать первым делом** — раздел «ЧТО ИЗУЧИТЬ ЗАРАНЕЕ» ниже.
6. **Чего не делать:** не трогать Creo (сессия занята первой ногой), не менять `creo_pdf`, не писать
   на `Z:`, ничего не удалять и не перемещать, в базы агента — только `SELECT`.

## ЦЕЛЬ
Дать дому список чертежей, у которых **нет своей модели**, чтобы PDF-рутина не считала их ошибками,
а показывала отдельной строкой «чертёж-сирота (нет модели)».

## ЭКОНОМИЯ (что не читать, что не искать, что уже есть)
- Creo НЕ трогать (сессия занята первой ногой). Вся работа — файлы и базы, только чтение.
- Не искать в интернете; не читать `PROGRESS_spec112.md` и историю репо.
- `creo_pdf` НЕ менять — это работа первой ноги. Новый инструмент — своя папка.
- На `Z:` не писать. Отчёты и логи — только в `D:\AI\log\...`.
- Уже есть (см. п.4 «Начальных данных»): `cmnm_scan.py`, `dup_scan\`, инвентарь `models` 44 154,
  `models_raw` 93 148, `search.pro` 4099 папок.

## ЧТО ИЗУЧИТЬ ЗАРАНЕЕ (скиллы и карты)
1. `company_conventions.md`, `MANIFEST.md`, `strategy.md`, `SKILL_index.md`, `SKILL_parallel_local_leg.md`.
2. `PDF\SKILL_pdf_routine.md` и `PDF\SKILL_pdf_index.md` — как устроена PDF-рутина.
3. `Creo\CREO_MAP.md`, `AGENT_MAP.md`, `agent\dev\PROGRAM_REGISTRY.md`.
4. `D:\AI\tools\agent\dup_scan\README.md` — образец паспорта программы класса Р.

## Ф0. ЦИТАТЫ (проверить на диске и привести вывод команд)
1. `Test-Path 'Z:\PTC\Work\УЧЕБА\Кирил\приваи.drw.1'` → True; рядом `приваи.prt*`/`приваи.asm*` нет.
2. `Test-Path 'Z:\PTC\Work\УЧЕБА\Кирил\ыва.asm.1'` → True (у `ыва.drw.1` модель есть — это НЕ сирота).
3. В `Z:\PTC\Work\000_31 Ремонты и доработки\ктп-800-1250-00 Корзина в термическую шахтную печь\КТП-800-1250_ Лазер\`
   лежат только `*.drw.1` и `*.dxf`, ни одного `.prt/.asm`.
4. `Z:\PTC\Work\search.pro` существует (4099 папок) — путь поиска настроен, дело не в нём.
5. В `agent.sqlite` таблица `models` = 44 154 строки; в `harvest.db` есть `models_raw` (93 148 строк).

## Ф1. ИНСТРУМЕНТ `orphan_scan` (класс Р, без Creo)
Папка `D:\AI\tools\agent\orphan_scan\`: `orphan_scan.py`, `orphan_scan.bat`, `README.md`.
Признак сироты для чертежа `X.drw[.N]` в папке P:
1) рядом (в P) нет `X.prt[.N]` и `X.asm[.N]`; И
2) имя `X` отсутствует в инвентаре моделей (`models.name` в `agent.sqlite` и `models_raw` в `harvest.db`).
Если имя в инвентаре есть — категория «модель есть в другом месте» (НЕ сирота, но помечается).
Понимать Creo-версии: `X.drw.1` → расширение `drw` (в доме это уже учтено в `dup_scan`).
Режим один: отчёт (ничего не двигаем, `--apply` не нужен). Логи: `D:\AI\log\orphan_scan\run_<дата>_<время>.txt`.
В базы агента НЕ писать (только `SELECT`).

## Ф2. ПРОГОН И ОТЧЁТ
- Прогон по `Z:\PTC\Work` (только чтение): всего чертежей, сирот, «модель в другом месте»,
  первые 50 сирот с полными путями, распределение сирот по верхним папкам `000_*`.
- В отчёт положить цитаты приёмки: `приваи` = сирота, `ыва` = не сирота, `00-06` = сирота.
- Отчёт человеку: `D:\AI\log\reports\REPORT_spec113_<нога>_<дата>.md`.
- Журнал: `D:\AI\repo\PROGRESS_spec113.md` (структура — раздел 6 в `SKILL_parallel_local_leg.md`).

## ПРИЁМКА (проверяет первая нога своими командами)
1. `orphan_scan.bat "Z:\PTC\Work\УЧЕБА\Кирил"` → `приваи` в списке сирот, `ыва` — нет.
2. `orphan_scan.bat "<папка КТП>"` → `00-06`, `00-10`, `00-12` — сироты.
3. Инструмент не пишет в базы агента и не запускает Creo (проверяется по коду и по процессам).
4. `README.md` с разделами «ОКНО/ДВИЖОК» и строкой `STATUS: PASSED`; строка в `PROGRAM_REGISTRY.md`.

## ЗАПРЕТЫ
Не удалять и не перемещать файлы; не менять `creo_pdf`; не писать на `Z:`; не «улучшать» базы агента;
не трогать чужую работу (чужие отчёты, чужие папки программ).