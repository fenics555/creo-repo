# SPEC 113 «Чертежи-сироты: список по дереву Work»
Источник: находки первой ноги 23.09.2026 при разборе 52 ошибок PDF-рутины (проба на живом Creo).
Исполнитель: параллельная локальная нога. Приёмка: первая нога (Cline). Спека не правится.

## НАЧАЛЬНЫЕ ДАННЫЕ (читать первым)
1. **Кто и где.** Ты — параллельная локальная нога (Ollama `gemma4:26b-131k`, окно 131k) с теми же
   правилами и скиллами, что у первой ноги. Приёмку делает первая нога (Cline).
2. **Дом.** Репо `D:\AI\repo` (скиллы, карты, спеки, индексы); агент `D:\AI\tools\agent` (тело и
   программы); логи `D:\AI\log\...`; рабочее дерево `Z:\PTC\Work` — **только чтение**; конфиги Creo
   `Z:\PTC\CREO-START\START-STD` (там боевой `config.pro`); базы `D:\AI\tools\agent\data\agent.sqlite`
   (после чистки 23.09 — 0,19 ГБ) и `harvest.db` (63 МБ).
3. **Что уже сделано по теме.** PDF-рутина дома работает (сканирует 11 901 чертёж, создаёт и обновляет
   PDF). Последний прогон дал **52 ошибки**; первая нога разобрала их на три причины: (а) **чертёж-сирота**
   — модели с тем же именем нет; (б) обрыв связи с Creo (`XToolkitCommError`); (в) попытка открыть файл
   с версией (`X.drw.1`) — Creo такие имена не принимает. Проба на живом Creo подтвердила: **чертёж без
   модели Creo не открывает** (`XToolkitNotFound`). Твоя работа — отделить сирот от настоящих ошибок.
4. **Что есть в наличии** (не изобретай заново):
   - `D:\AI\tools\agent\cmnm_scan\cmnm_scan.py` — разбор заголовков Creo-файлов (поле `#- CMNM`), уже
     учитывает служебный префикс сессии (`приваи.drw.1` → `010приваи.drw`);
   - `D:\AI\tools\agent\dup_scan\` — образец программы класса Р (папка + `*.bat` + README + лог + приёмка);
   - `agent.sqlite`, таблица `models` = **44 154** строки (name, ext, path) — инвентарь моделей;
   - `harvest.db`, `models_raw` = **93 148** строк (name, ext, path, mtime, size, sha256head), `pairs` = 24 607;
   - `Z:\PTC\Work\search.pro` — **4099** папок (пути поиска Creo), файл существует;
   - три контрольных случая для приёмки: `приваи` — сирота; `ыва` — не сирота (рядом `ыва.asm.1`);
     `00-06` в папке КТП — сирота (там только `*.drw.1` и `*.dxf`).
5. **Что прочитать первым делом** — раздел «ЧТО ИЗУЧИТЬ ЗАРАНЕЕ» ниже.
6. **Чего не делать:** не трогать Creo (сессия занята первой ногой), не менять `creo_pdf`, не писать
   на `Z:`, ничего не удалять и не перемещать, в базы агента — только `SELECT`.

## ЦЕЛЬ