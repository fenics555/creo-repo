---
name: spec-for-parallel-leg
system: общее
description: Use when: пишешь спеку для ПАРАЛЛЕЛЬНОЙ локальной ноги (Ollama gemma4:26b-131k) или принимаешь её отчёт
when: спека для ноги, параллельная нога, отчёт ноги
priority: high
---
# СПЕКА ДЛЯ ПАРАЛЛЕЛЬНОЙ ЛОКАЛЬНОЙ НОГИ (и приёмка её отчётов)

Нога — локальный агент (Ollama `gemma4:26b-131k`, окно 131k) с теми же правилами, клайнрулами и
скиллами, что у первой ноги. Первая нога (Cline) **пишет спеку**, нога **исполняет**, первая нога
**принимает и учит**. Спека живёт в репо: `SPEC_<N>_<имя>.md`; журнал — `PROGRESS_spec<N>.md`.

## 1. НАЧАЛЬНЫЕ ДАННЫЕ (первый раздел ЛЮБОЙ спеки — без него нога гадает)
Обязательный минимум:
1. **Кто и где**: нога, её модель/окно, кто принимает работу.
2. **Что такое дом** (2–4 строки): репо `D:\AI\repo` (скиллы, карты, спеки), агент `D:\AI\tools\agent`
   (тело + программы), логи `D:\AI\log\...`, рабочее дерево `Z:\PTC\Work` (только чтение),
   конфиги и шаблоны Creo `Z:\PTC\CREO-START`, базы `agent\data\agent.sqlite` и `harvest.db`.
3. **Что уже сделано по теме** — числами и фактами, а не «в целом» (пример 23.09.2026: база знаний
   6,38 → 0,19 ГБ, все чанки удалены, поиск на FTS5; CREOSON поднимается по требованию; чесалка читает
   эталоны из config.pro; 52 ошибки PDF-рутины разобраны на три причины).
4. **Что есть в наличии** — готовые инструменты, образцы, файлы, таблицы (с числами строк), чтобы нога
   не изобретала заново.
5. **Что прочитать первым делом** — точный список путей (скиллы, карты, индексы).
6. **Чего не делать** и почему (запреты — из законов дома).
Правило: если нога не может начать без вопроса — начальных данных не хватило, это долг спеки, а не ноги.

## 2. ФАЗЫ, ЯКОРЯ, ЭКОНОМИЯ
Ф0 — цитаты с диска (команды + их вывод, без пересказа); Ф1..Фn — работа. В каждой фазе — точные якоря:
путь, имя файла, строка/`old_text`, ожидаемый результат. Поле «экономия»: что не читать, что не искать,
что уже существует. Приёмка — команды и ожидаемый результат, включая негативные проверки.

## 3. ЧТО НУЖНО УТОЧНЯТЬ (перед началом работы)
- Свободен ли Creo (закон одной сессии) и нужен ли он этой работе вообще.
- Какие корни/папки входят в работу, какие исключены (`_trash`, `backup`, архивы, чужие папки).
- Что считать истиной при расхождении: инвентарь базы, диск или заголовок файла.
- Как называть новые сущности: папка программы, `*.bat`, лог, отчёт — по паспорту класса Р.
- Границы: где нога останавливается и спрашивает человека (записи на Z:, удаления, Creo на запись).

## 4. ЧТО МЕНЯТЬ В ВЫВОДАХ СПЕК (пополняется после каждого отчёта ноги)
- 23.09.2026, старт: первая спека — `SPEC_113_orphan_drawings.md`. Урок сразу: одной «экономии» мало,
  нужен явный раздел «Начальные данные» (см. п.1) — введён.

## 5. ПРАВИЛА НОГИ (исполнять обязательно)
- **Z: — только чтение.** Писать можно только в `D:\AI\...` (исключение даёт человек словом).
- **Creo — одна сессия на машину.** Нужен Creo — сначала спроси: «сессия свободна?».
- **Ничего не удаляем:** мусор → `_trash*`/`_trash_dup`, копии → `pre_<метка>`, решение — за человеком.
- **Чужое — только чтение:** чужие базы, файлы, отчёты, папки программ не правим.
- **Логи** — `D:\AI\log\<имя>\`; отчёты человеку — `D:\AI\log\reports\`.
- **Новый инструмент** = папка + `*.bat` (ОКНО) + самодостаточный движок (ДВИЖОК) + README с разделами
  «ОКНО/ДВИЖОК» и строкой `STATUS: PASSED`; строка в `agent\dev\PROGRAM_REGISTRY.md`.
- **Программа не импортирует агент** и не тянет его базы в память (урок 23.09.2026: 13 ГБ коммита).
- Спека не правится: расхождения — флагами в `PROGRESS_spec<N>.md`.
- Бюджет попыток: два отказа одного хода = смена метода; третья попытка тем же путём запрещена.

## 6. ОТЧЁТ НОГИ И ПРИЁМКА
`D:\AI\repo\PROGRESS_spec<N>.md`: (1) ЦИТАТЫ Ф0 (команды и вывод, без пересказа); (2) сделано по фазам
с точными путями и числами; (3) ПРИЁМКА (команда + результат, включая негативные проверки);
(4) ЧТО НЕ СДЕЛАНО и почему — честно, без «почти»; (5) УРОКИ — что нога узнала о доме и о себе.
Плюс отчёт человеку: `D:\AI\log\reports\REPORT_spec<N>_<нога>_<дата>.md`.
Первая нога перепроверяет приёмку своими командами и пишет вердикт: **ПРИНЯТО / ПРИНЯТО С ЗАМЕЧАНИЯМИ /
НЕ ПРИНЯТО** + чему учить (правки в этот скилл и в скиллы по теме).
