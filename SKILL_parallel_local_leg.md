---
name: parallel-local-leg
system: общее
description: Use when: ставишь работу ПАРАЛЛЕЛЬНОЙ локальной ноге (Ollama gemma4:26b-131k) или исполняешь её — как читать спеку, что изучить, как отчитаться
when: параллельная нога, parallel leg, спека для ноги, отчёт ноги
priority: high
---
# ПРАВИЛА ПАРАЛЛЕЛЬНОЙ ЛОКАЛЬНОЙ НОГИ

Нога — локальный агент (Ollama `gemma4:26b-131k`, окно 131k) с теми же правилами, клайнрулами и
скиллами, что у первой ноги. Это не «младший брат», а вторая рука дома: берёт небольшие
самодостаточные работы, пока первая нога занята Creo/PDF. Файл пополняется после каждого отчёта
(раздел «Уроки»).

## 1. Что нога получает и чего не делает
- Получает спеку в репо: `SPEC_<N>_<имя>.md` (формат дома: Ф0 цитаты → Ф1..Фn, точные якоря,
  обязательное поле «экономия»).
- Задачу сама не ищет и не расширяет. Расхождение спеки с диском — флаг в `PROGRESS_spec<N>.md`,
  сама спека не правится.

## 2. Первые 10 минут ноги (обязательный порядок чтения)
1. `company_conventions.md`, `MANIFEST.md` (законы дома), `PASSPORT.md`.
2. `strategy.md` — где дом сейчас, что близко/далеко.
3. `SKILL_index.md` → индекс нужного направления.
4. Скиллы по теме спеки (список указан в самой спеке).
5. `agent\dev\PROGRAM_REGISTRY.md` — какие программы уже есть, чтобы не плодить дубли.
6. `SKILL_parallel_local_leg.md` (этот файл) — правила ноги.

## 3. Законы, обязательные для ноги
- **Z: — только чтение.** Писать можно только в `D:\AI\...` (исключение даёт человек словом).
- **Creo — одна сессия на машину.** Нужен Creo — сначала спроси: «сессия свободна?».
- **Ничего не удаляем:** мусор → `_trash*`/`_trash_dup`, копии → `pre_<метка>`, решение — за человеком.
- **Чужое — только чтение:** чужие базы, файлы, отчёты не правим.
- **Логи** — `D:\AI\log\<имя>\`; отчёты человеку — `D:\AI\log\reports\`.
- **Новый инструмент** = папка + `*.bat` (ОКНО) + самодостаточный движок (ДВИЖОК) + README с
  разделами «ОКНО/ДВИЖОК» и строкой `STATUS: PASSED` после приёмки; строка в `PROGRAM_REGISTRY.md`.
- **Программа не импортирует агент** и не тянет его базы в память (урок 23.09.2026: 13 ГБ коммита).
- Бюджет попыток: два отказа одного хода = смена метода, третья попытка тем же путём запрещена.

## 4. Отчёт ноги (обязательные части)
`D:\AI\repo\PROGRESS_spec<N>.md`:
1. **ЦИТАТЫ Ф0** — что нога увидела на диске: команды и их вывод, без пересказа.
2. **Сделано по фазам** — точные пути и числа.
3. **ПРИЁМКА** — как проверено (команда + результат), включая негативные проверки.
4. **ЧТО НЕ СДЕЛАНО** и почему — честно, без «почти».
5. **УРОКИ** — что нога узнала о доме и о себе (переносится в «Уроки» ниже).
Плюс отчёт человеку: `D:\AI\log\reports\REPORT_spec<N>_<нога>_<дата>.md`.

## 5. Как нога принимается
Первая нога читает `PROGRESS`, перепроверяет приёмку своими командами и пишет вердикт:
**ПРИНЯТО / ПРИНЯТО С ЗАМЕЧАНИЯМИ / НЕ ПРИНЯТО** + чему учить (правки в этот файл и в скиллы).

## 6. Уроки (пополняется после отчётов)
- 23.09.2026: стартовая настройка. Правила выданы, отчётов пока нет. Первая спека — `SPEC_113`.
