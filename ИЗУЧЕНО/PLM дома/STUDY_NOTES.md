# Конспект изучения: PLM дома

**Статус:** живое чтение, начат 25.09.2026 08:58 (agent).
**Источник:** D:\AI\tools\agent\plm_tools.py + data\agent.sqlite + Z:\PTC\CREO-START\НАСТРОЙКИ\ШАБЛОНЫ
**Цель:** понять, что по PLM готово, а что нет; научиться читать историю изменений файла без мусора в рабочих папках
**Карта скиллов дома:** `D:\AI\repo\SKILL_index.md` — сверить, чтобы не выдумывать заново.

**Где живёт конспект:** `D:\AI\repo\ИЗУЧЕНО\<тема>\STUDY_NOTES.md` (память дома под гитом). Источники для чтения —
в `D:\AI\ИЗУЧИТЬ` (большие библиотеки вне гита).

**Как ведём записи (правило дома, 24.09.2026):**
1. важное из куска — `study_note` (запись ДО следующего куска);
2. **что не получилось — `study_fail` СРАЗУ** (выбрал не тот инструмент, вылет, ошибка, пустой ответ);
3. ход по папке — `study_progress` (файл за файлом, папка за папкой: что просмотрено, что дальше);
4. на каждой записи вопрос «нужен ли скилл?» — `study_skill` (заготовки в `D:\AI\repo\BACKLOG_tools.md`).
Временное (выборки, дампы, сырые выводы проб) — в урну `D:\AI\log\urn\agent\PLM дома\`.

---

<!-- ПРОГРЕСС -->
**Ход изучения:** просмотрено: разведка PLM + **закрыт вопрос чтения истории**: история читается прямо из файла модели (без Creo и CREOSON), собран инструмент `plm_reader` (окно + CLI, таблица изделий и история изменений) · дальше: по слову хозяина — привязать к реестру (`plm_revisions`/`plm_changes`) и написать `dev\plm_check.py` · _обновлено 25.09.2026 14:40 (agent)_
<!-- /ПРОГРЕСС -->

## 1. ЧТО ГОТОВО: свой реестр PLM + ревизия через CREOSON
_25.09.2026 08:58, agent_

plm_tools.py (7 инструментов): plm_mine (наполнить реестр изделий+BOM из XML), plm_item, plm_bom, plm_where, plm_lifecycle (статус, approval), plm_ii (извещение об изменении ГОСТ 2.503, approval), plm_audit (28885 изделий, 19095 без материала). Таблицы дома: plm_items, plm_bom, plm_revisions, plm_changes в data\agent.sqlite (префикс plm_ — закон дома). Чужие bom(36811), usage(2400), models(44154) не тронуты (проверено 23.09, бэкап базы есть). Руководство: repo\GUIDE\plm.md. Ревизия модели: CREOSON file:get_fileinfo → revision ✓. JLink pfc: GetRevision/GetVersionStamp есть, истории операций в pfc-API НЕТ. windchill-спеки CREOSON есть, но Windchill в доме нет.

**Нужен ли скилл?** нет (правило пока не выкристаллизовалось)

## 2. ЧЕГО НЕТ: чтение «Истории файла» — и что выяснено про саму историю
_25.09.2026 08:58, agent_

История хранится ВНУТРИ модели (сервер не нужен): в liteika_hts_mm.asm.1 — 198 записей сохранения, поля: typed_data(MdlStored) = «Модель сохранена», rev_string = номер ревизии, comment (UTF-8, «Наименование компьютера: 'frezer-4'»), Proe_version (Creo 12.4.2.0), hostid, from_mdl_name/to_mdl_name (переименования), таблица пользователей (USER), modif_time + tm_sec/min/hour/mday/mon/year. Отчёт «экран» Creo рендерит из ВРЕМЕННОГО файла: сегодня 08:57 в %TEMP% были mtrack_report.xml (31 КБ) и mtrack_report_proephis_1.html (113 КБ) — и исчезли через минуты (Creo убирает за собой). Значит «экран» рабочие папки не мусорит (а «в файл» — мусорит, как хозяин и сказал). Мой грубый парсер вытащил только часть: запись — бинарный объект с тегами.

**Нужен ли скилл?** нет (правило пока не выкристаллизовалось)

## 3. ПЛАН ЧТЕНИЯ ИЗМЕНЕНИЙ (два пути, оба без мусора)
_25.09.2026 08:58, agent_

A. Наблюдатель TEMP: агент просит Creo показать отчёт (CREOSON interface:mapkey), ловит %TEMP%\mtrack_report.xml / mtrack_report_proephis_*.html, копирует в урну и разбирает (XML/HTML), временный файл Creo удалит сам. Плюс: просто и быстро. Минус: нужны запущенный Creo и CREOSON, файл надо поймать живым. B. Свой парсер файла модели (.prt/.asm): история без Creo, можно обойти склад; нужен аккуратный парсер и докопать формат поля time. Лёгкий вариант: ревизия через file:get_fileinfo (только номер). Предложено: plm_history (путь A), plm_history_file (путь B), наполнение plm_revisions/plm_changes, проверка dev\plm_check.py. Проверка на примере хозяина: liteika_hts_mm.asm.1 — ревизии 718/713/712/710/708, USER, даты 08/05-08/04)22 26.

**Нужен ли скилл?** нет (правило пока не выкристаллизовалось)

## Кандидаты в скиллы

- plm_history · **готов** (25.09.2026) · история изменений читается прямо из файла модели: `D:\AI\tools\plm_reader\plm_reader.py` — окно «История выбранного»/«История по папке» и CLI `--history`; сверено с отчётом хозяина (ревизии 647…718, `USER`, `frezer-4`). Путь через `%TEMP%\mtrack_report*` не понадобился (отчёт Creo живёт в TEMP и исчезает; файловый путь проще и не требует Creo). Осталось: `dev\plm_check.py`, наполнение `plm_revisions`/`plm_changes`
