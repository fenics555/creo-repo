# КУЛЬТУРА ДОМА: куда что кладётся (одно место)

**Обновлено:** 24.09.2026 (Cline). **Канон в правилах:** `D:\AI\repo\.clinerules`, абзац «ОТЧЁТЫ И ЛОГИ».
Этот файл — карта. Если правило и карта разошлись — правь оба, а расхождение пиши в отчёт.

## 1. Пять адресов

| Что | Куда | Срок жизни | Кто убирает |
|---|---|---|---|
| Логи программ | `D:\AI\log\<имя программы>\` | 56 дн. (agent, harvest, purge, cleaner), остальные 30 дн. | `log_clean` |
| Отчёты | `D:\AI\log\reports\REPORT_<задача>_<исполнитель>_<дата>.md` | 28 дн. | `log_clean` |
| Временное (пробы, патчи, дампы) | `D:\AI\log\urn\<исполнитель>\` | 14 дн. | `log_clean` |
| Память дома (скиллы, спеки, манифест, паспорт, заготовки, **конспекты**) | `D:\AI\repo\` под гитом | вечно | никто |
| **Программы дома** (инструменты) | `D:\AI\tools\agent\<имя>\` (движок + `.bat` + свой `README.md`), служба — `D:\AI\tools\` (GIT_SYNC, STOP_ALL, `data\`) | вечно | никто |
| Конспект изучения | `D:\AI\repo\ИЗУЧЕНО\<тема>\STUDY_NOTES.md` | вечно (пока не стало скиллом) | никто |
| Источники для чтения (библиотеки, архивы) | `D:\AI\ИЗУЧИТЬ\<тема>\` (**вне гита**) | вечно | никто |

Исполнители в урне: `cline`, `agent`, `user` (и любые другие по имени исполнителя) —
папки создаются сами при первой записи, пустых не держим.
Сроки — в `D:\AI\log\retention.json` (один файл на всех: его же читает ночной цикл агента).
Ключи: `urn` 14, `root` 14 (файлы, брошенные прямо в корень `log`), `reports` 28, `agent/harvest/purge` 56,
остальным каталогам — срок по умолчанию (30 дней). Считаются и ВЛОЖЕННЫЕ папки (`urn\cline\…`, `reports\<тема>\…`).
Корзина уборки — `D:\AI\log\_trash_clean\<дата>\` (7 дней, можно вернуть). Вне `D:\AI\log` уборщик не трогает ничего.

## 2. Что считается временным (перечень, чтобы не гадать)

1. скрипт-писатель и патч (`.py .ps1 .cmd .bat`) — одноразовая проба;
2. вывод пробы: `out.txt`, `err.txt`, `*_run.txt`, `*_err.txt`, снимки `*.json`;
3. выборка из большого файла (страницы PDF, дамп grep по логу, OCR) — `urn\<исполнитель>\<тема>\`;
4. копия чужого лога для разбора (оригинал живёт в `log\<программа>\`);
5. песочница с тестовыми файлами (`test.prt`, `test.asm`, учебная папка) — `urn\<исполнитель>\sandbox\<метка>\`;
6. промежуточные выгрузки/снимки БД — туда же.

**НЕ временное (в урну не идёт):**
- копия файла перед правкой — `D:\AI\tools\agent\data\backup\pre_<метка>_<файл>` (это откат, не чистится);
- конспект изучения `STUDY_NOTES.md` — это память: живёт в `repo\ИЗУЧЕНО\<тема>\` под гитом
  (не в `ИЗУЧИТЬ`: тот каталог **вне гита**, 24.09.2026 оттуда исчезли две библиотеки «Новые правила*»);
- рабочие файлы запускателя (`agent\data\tmp\agent_err.txt` и т. п.) — их пишет `ctl.py`, это не мусор ИИ;
- базы дома, `repo\`, `ИЗУЧИТЬ\`, `data\backup\` — не чистится никогда.

## 3. Конспект изучения (метод «изучил кусок — записал»)

1. `study_start topic=<имя> source=<папка/файл> why=<цель>` — заводит `D:\AI\repo\ИЗУЧЕНО\<тема>\STUDY_NOTES.md`
   (память дома под гитом) с шапкой (источник, цель, где живёт конспект, правила ведения записей) и разделом «1.».
2. Прочитал кусок → **сразу** записал:
   `study_note тема=<имя> кусок=<что читал> текст=<что понял, что осталось>`.
   Правило: запись ДО следующего куска. На 300 страницах «потом запишу» = потерял.
3. В каждой записи один вопрос: **нужен ли скилл?**
   - да → `study_skill тема=<имя> скилл=<имя> зачем=<почему> готовность=<замысел|черновик|готов>`;
   - нет → строкой в записи: «кандидат в скилл: нет, потому что …».
4. `study_status` — что начато, где остановился, какие заготовки ждут.
5. Заготовка «готов» = скилл `SKILL_*.md` в репо → попадает в карту `repo\SKILL_CHARGE.md` (её читает агент).

## 4. Проверки и пробы (dev)

- вывод пробы — в урну: `D:\AI\log\urn\cline\<тема>_<что>.txt` (в `log\reports` только отчёты!);
- отчёт ноги/задачи — `D:\AI\log\reports\REPORT_<задача>_<исполнитель>_<дата>.md`, пишется ДО последней строки чата;
- последняя строка чата — указатель на отчёт;
- проверки дома: `dev\ui_check.py`, `dev\skills_check.py`, `dev\stop_button_check.py`,
  `dev\study_check.py` (конспект и неудачи), `dev\culture_check.py` (этот порядок адресов) —
  все пишут вывод только в урну;
- **следит за порядком** `dev\culture_check.py`: 12 проверок (отчёты, корень log, урна, временное вне урны,
  конспекты в памяти дома, совпадение копий `.clinerules`, мёртвые ключи `retention.json`, пустые папки)
  → код возврата 1 при нарушении. Прогонять после уборок и «разборов» папок.
