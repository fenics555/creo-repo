---
name: python-standard
system: Python
description: Стандарт расчётных Python-скриптов проекта (блоки, суффиксы единиц, аудит A1–A15). К relations Creo НЕ применяется
when: python, стандарт, расчёт, скрипт, блоки, суффиксы, аудит, префиксы, архитектура скрипта
priority: high
---

# СТАНДАРТ РАСЧЁТНЫХ PYTHON-СКРИПТОВ
Область: Python-скрипты расчётов и автоматизации в VS Code.
К relations внутри Creo НЕ применяется — там верховенствует SKILL_relations_constitution.

## 1. Блочная архитектура (>100 строк — обязательно)
Блоки последовательные, с номером и именем. Порядок:
INPUT → SPLAV/MAT → CONST → SAFE → расчётные → ECO → REPORT/VERDICT.
- Запрещено использовать внутренние переменные другого блока.
- Только явные выходы с префиксом блока-источника.
- Блок самодостаточен: удаление промежуточного не роняет код.

## 2. Имена: ПРЕФИКС_ОПИСАНИЕ_СУФФИКС_ЕДИНИЦЫ
Префикс = имя блока. Суффикс — единица (в Python единицы неизвестны):
_MM, _MM2, _MM3, _M, _M_S, _KG, _SEC, _H, _FRAC, _RATIO, _TEXT.
Пример: PRIB_V_TOTAL_MM3, HYDRO_Q_MM3_S, ECO_KIM_FACT_FRAC.

## 3. Уравнения
- Переменная не используется до объявления (порядок сверху вниз).
- Каждое деление/корень — с защитой: if denom > 0 else 0.
- Магические числа запрещены — только именованные константы с источником.
- В одном расчёте — одна система единиц (все мм или все м).
- Два эмпирических коэффициента на одну величину запрещены.

## 4. Аудит A1–A15
A1 размерность; A2 деление на ноль; A3 корень >= 0; A4 порядок определения;
A5 префиксы; A6 суффиксы единиц; A7 магические числа; A8 двойные коэффициенты;
A9 независимость блоков; A10 неиспользуемые переменные; A11 масштабирование;
A12 физический смысл; A13 повторные вычисления; A14 защита после вычитаний;
A15 округление только на финале.
Критичность: A1,A2,A3,A4,A8,A11 — CRITICAL; A5,A6,A7,A9,A12,A13,A14 — MAJOR; A10,A15 — MINOR.

## 5. Чистота
После правки *.py: python -m pyflakes . → 0; python -m compileall -q . → 0.
TEST-first для внешних связок — см. SKILL_test_first_rule.