name: crash_sleep-poll-loop-after-timeout
system: CRASH
description: Use when: исполнитель после таймаута run_commands (30 с) ждёт детач-процесс повторными опросами «Start-Sleep 25 + проверка файла» — и зацикливается на 5+ одинаковых ходах вместо смены метода и отчёта. Закон дома — MANIFEST.md (М7, М10).
when: sleep-poll, петля опросов, детач-процесс, Start-Sleep, таймаут 30с, зацикливание, crash_plan-loop-replan-no-exec
date: 17.09.2026
executor: Cline (локальный, gemma4:26b)
task: СПЕКА 66б (критическая), Ф4/Ф5 приёмка
СЧЁТЧИК: заменён на ПОВТОРЫ по шаблону конституции
ПОВТОРЫ: 1
ОШИБКА (дословно, для grep):
Command failed: Command was aborted
Command timed out after 30000ms
СИМПТОМ: после таймаутов run_commands исполнитель ждёт детач-процесс повторными ходами «Start-Sleep 25 + проверка файла» — 5+ одинаковых опросов подряд, результата нет.
ПРИЧИНА: index_all по сетевым корням идёт минуты; длительность не оценена ДО запуска, план Б не подготовлен; М7 (смена метода после двух отказов) не сработал.

## ЧТО ДЕЛАЛ
По спеке 66б запускал приёмочные пробники детачем (Start-Process python ... -WindowStyle Hidden, результат в файл). Пробник prok66b2.py делал синхронный scanner.index_all() по живым корням из kb_roots.txt, среди них сетевой Z:\ — время непредсказуемо.

## ЧТО ИСПОЛЬЗОВАЛ
- Start-Process python детачем + RedirectStandardOutput/Error в data\tmp (сам запуск — правильный приём);
- далее для ожидания результата: 7 раз подряд один и тот же ход
  `Start-Sleep -Seconds 25; if (Test-Path ...out.txt) {...} else {'still running'}`
  в run_commands.

## ПОЧЕМУ НЕ ПОЛУЧАЛОСЬ
1. run_commands имеет жёсткий таймаут 30 с — «Start-Sleep 25 + опрос» съедал почти весь бюджет (crash_command-30s-timeout), часть вызовов падала с «Command timed out» / «aborted».
2. index_all по сетевым корням Z:\ шёл минуты — файл результата не появлялся, и каждый опрос давал «still running».
3. После ДВУХ отказов одного пути (М7) я не сменил метод, а повторил тот же ход ещё 5 раз — прямое нарушение §1.5 и §4.4 скилла выживания. Подпись — та же, что у crash_plan-loop-replan-no-exec: «план повторяется, шаг не выполняется».
4. Пользователь прервал словами «у тебя зацикливание».

## ПРОФИЛАКТИКА
1. ПРОТОКОЛ ОЖИДАНИЯ ДЕТАЧ-ПРОЦЕССА (главное): запустил детач → ОДИН контрольный опрос через 20-25 с. Результата нет → стоп-отчёт («запущено, PID, где будет результат, приёмка — в следующей ноге»). Второй опрос того же файла ЗАПРЕЩЁН.
2. ТЯЖЁЛЫЕ ПРИЁМКИ НЕ ГНАТЬ ПО ЖИВЫМ КОРНЯМ: для index_all-подобных проверок делать временный корень (папка с 1-2 файлами) и подменять kb_roots.txt на время прогона, возвращая оригинал в finally. Приёмка укладывается в секунды и в один вызов run_commands.
3. ПЕРЕД запуском шага дольше ~20 с — оценить длительность ДО старта и иметь план Б (лог, изолированный корень, частичная приёмка). Нет оценки = не запускать без детача и сразу отчёт.
4. ПРИЁМКА, КОТОРАЯ НЕ ИДЁТ, идёт в «НЕ СДЕЛАЛ и почему» (11.1) — это её штатное место, а не поражение. Закон дома выше желания красиво закрыть спеку.
5. СЧЁТЧИК ПОПЫТОК ВЕСТИ ЯВНО: перед каждым повтором хода спросить себя «это уже 2-я попытка того же хода?» — если да, смена метода или отчёт (М7). Счётчик «в голове» не работает.

## ЧТО НАРУШЕНО (сводка)
- М7 (.clinerules): «два отказа одного пути = смена метода; третья попытка тем же путём запрещена» — сделано 7 попыток.
- SKILL_local_agent_cline §1.5: «>20 вызовов на ногу — остановка» — петля съела бюджет без остановки.
- SKILL_local_agent_cline §4.4: «упавший шаг дословно не повторять» — повторён 5 раз после первых двух падений.
- SKILL_local_agent_cline §7 п.4 (чек-лист «команда уложится в 25 с?») — учтён формально, план Б не подготовлен.

## ПОЧЕМУ НАРУШЕНО
- Ошибка классификации: «висящий детач-процесс» не был распознан как крах-сценарий §4.1 («необъяснимый отказ = отчёт»); считался «нормальным ожиданием».
- Пробел в скилле: готового протокола ожидания фонового процесса не было — вместо запроса протокола у пользователя сделана импровизация.
- Мотивационный сбой: критическая спека + нежелание сдаваться с непринятой приёмкой → давление «закрыть любой ценой» вместо честного «НЕ СДЕЛАЛ».
- Счётчик попыток не фиксировался письменно → М7 «не заметил».

## ВРЕМЯ НАРУШЕНИЙ (17.09.2026)
- 12:19:26 / 12:19:19 — правки agent.py / scanner.py (не нарушения, якорь состояния);
- 12:20:35 — рестарт агента принят (PID 15832);
- ~12:22-12:24 — НАРУШЕНИЕ №1: первый цикл опросов (5 вызовов «sleep 25 + проверка»), один прерван пользователем;
- 12:27:52 — старт prok66b2.py (PID 13412);
- ~12:28-12:35 — НАРУШЕНИЕ №2: второй цикл опросов (6+ вызовов того же хода, один «aborted»);
- ~12:35 — стоп пользователем: «у тебя зацикливание».

## ЧТО ПРЕДПРИНЯТЬ, ЧТОБЫ НЕ ПОВТОРЯЛОСЬ
1. Внести в SKILL_local_agent_cline §2 протокол ожидания из «ВЫВОД» п.1 и в §7 чек-лист два вопроса: «шаг дольше 60 с — план Б есть?» и «это 2-я попытка того же хода?» (ждёт слова пользователя — правка скилла).
2. При каждой приёмке тяжёлого кода — временный тестовый корень, не живые сетевые диски (ВЫВОД п.2).
3. Профилактика при рецидиве: прочитать этот скилл ДО хода; счётчик этой подписи увеличить; 3+ — пользователю на разбор (М10).
