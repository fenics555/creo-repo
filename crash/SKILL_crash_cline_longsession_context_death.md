name: crash_cline_longsession_context_death
system: CRASH
description: Use when: окно Cline умирает молча после длинной сессии на шаге генерации длинного ответа
when: cline, вылет, context, longsession, компакция, papers, прогресс
date: 19.09.2026
executor: Cline (GLM)
task: спека 98, Ф4 (бумаги после Ф1-Ф3)
ОШИБКА (дословно, для grep):
<traceback отсутствует; окно Cline прервалось без сообщения, ответ оборван на фразе «Пишу прогресс и отчёт-файл:»>
СИМПТОМ: сессия прожила 2 спеки подряд (84→98), ~60 вызовов инструмента, большие файлы в контексте; смерть наступает на шаге генерации длинного финального ответа, повторно — на том же шаге
ПРИЧИНА: гипотеза — исчерпание контекста/памяти extension host (окно ~131k переполнено), либо сбой VS Code на длинной генерации; закономерность: бумаги (progress/REPORT) копились до конца ноги и не были записаны на диске до вылета
ПРОФИЛАКТИКА:
1. Бумаги (progress_specNN.md блок фазы + REPORT в log\reports) писать СРАЗУ после закрытия фазы, а не после git-фикса; вылет между фазами не теряет работу.
2. Лимит ноги ≤20 вызовов соблюдать жёстко; 15+ вызовов = HANDOFF в progress-файл и передача.
3. После каждого git-фикса — 2-строчный блок в progress-файл тем же сообщением.
4. Большие файлы (>3 КБ) — только частями: create + дописывание у последней строки (не один new_text).
5. Длинные финальные отчёты — сначала в файл (REPORT), потом короткая строка в чат: смерть окна после записи файла не теряет отчёт.
ПОВТОРЫ: 3
РЕЦИДИВ 22.09.2026 (окно 1790075839950_5r7bm): сессия снова встала на длинной ноге
(изучение Давыдовки: 90 tool_use, лог вырос 273 → 581 КБ; пакетное чтение ~30 скиллов
+ рекурсивные листинги с обрезкой ~48 КБ). Точного traceback текущего обморока нет; есть
свидетельство инфраструктуры: hooks.jsonl, session_shutdown reason=failed_external_process_exit
для прошлой сессии 1789449329358_v9kwq, повтор через source=stale_session_reconciler 10 раз
(10:42–10:45Z, локально ~13:42–13:45).
ДОБАВКА К ПРОФИЛАКТИКЕ (22.09.2026):
6. За один ход — 3-6 точечных чтений, НЕ пачки десятков файлов; рекурсивные листинги
   фильтровать (-Include/-Depth) и считать, а не печатать простыню.
7. Бумаги (конспект/скилл) писать на диск СРАЗУ по мере готовности, не одним финальным
   блоком — при обмороке готовое остаётся на диске.
8. После обморока первым делом читать hooks.jsonl (session_shutdown/reason, source) —
   отличать смерть внешнего процесса (failed_external_process_exit) от зависания окна.
ЭСКАЛАЦИЯ: счётчик 3 → вынести пользователю на разбор (проверить лимит контекста/памяти
extension host; дробить тему на ноги ≤15-20 вызовов).
