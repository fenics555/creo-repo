name: crash-migration_tool_result_missing
system: CRASH
description: Use when: search_codebase падает с ошибкой миграции контекста или повторяется
  «tool result missing» в длинной сессии
when: migration, tool result missing, legacy conversation history, search_codebase
date: 16.09.2026
executor: Cline (glm-5.3-flash)
task: спека 44в «руки доказывают себя», сессия-продолжение
ОШИБКА (дословно, для grep):
[migration] Tool result missing in legacy conversation history.
СИМПТОМ: исполнитель оборвался посреди задачи без полезного ответа; при оживлении та же
строка на вызове search_codebase.
ПРИЧИНА: гипотеза — слой оркестрации не находит результат инструмента в легаси-истории
сессии при миграции контекста (отчёт исполнителя от 16.09.2026); факт — ошибка повторяется
на длинных сессиях с большой историей вызовов инструментов.
ПРОФИЛАКТИКА:
1. Повторить вызов: ошибка транзиентная, чаще всего уходит со второй попытки.
2. Запросы в search_codebase гонять по одному, не массивом queries.
3. При системных повторах проверить стабильность окружения и инструментов.
4. Длинные задачи дробить на сессии с якорем-хэндоффом в начале каждой.
ПОВТОРЫ: 2 (первый и второй — 16.09.2026)
ЧТО ДЕЛАТЬ СЕЙЧАС: продолжить задачу с якоря-хэндоффа; search_codebase начинать с одного
запроса; при повторе 3 в этой сессии search_codebase не использовать, искать findstr по файлам.