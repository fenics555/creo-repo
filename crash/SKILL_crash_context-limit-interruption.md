name: context-limit-interruption
system: CRASH
description: Use when: выполнение задачи прерывается из-за ограничений инструментов (search_codebase, context window) или таймаутов в процессе миграции/распила.
when: search_codebase timeout, context full, tool limit reached, "вылетел"
date: 17.09.2026
executor: Cline
task: Spec 71 (распил agent.py)

ОБЪЯСНЕНИЕ (дословно, для grep):
вылетел (или "инструмент прерван", "limit reached")

СЮЖЕТ:
Прерывание работы агента из-за того, что инструменты (чаще search_codebase) не могут обработать запрос в текущем состоянии миграции, либо контекст переполнен. Агент воспринимает это как остановку/вылет.

ПРИНЯТИЕ:
1. Проверить наличие бекапов (pre_phase_*.bak).
2. Сообщить состояние (что уже сделано, на каком шаге остановились).
3. Использовать якорь для передачи следующей ноге.

ПОЛУЧЕНИЕ «Е»:
1. Проверить, что файлы в текущем состоянии (не повреждены).
2. Если файлы повреждены — git checkout из последнего autosave.
3. Продолжить с того же шага, используя цитату из текущего состояния.
