name: crash_destructive-probe-rerun
system: CRASH
description: Use when: разрушительная проба (kill_creo, delete, erase, перезапись) исполнена ПОВТОРНО из оставшегося в tmp скрипта
when: kill_creo, destructive, probe, rerun, tmp, повтор, разрушительная проба, Creo
date: 17.09.2026
executor: Cline (облачный)
task: тема CREOSON/сессии, проба убийства Creo (kill_creo)
ОШИБКА (дословно, для grep):
is_creo_running before kill: {"running": true} → kill_creo status: {"error": false} →
is_creo_running after kill : {"running": false} → creo:pwd: "No session found"
СИМПТОМ: один и тот же разрушительный вызов исполнен ДВАЖДЫ. Первый раз — по прямому слову
пользователя (проба знания). Второй раз — случайно: скрипт `probe_b_kill.py` остался в
`data\tmp`, и «проверка состояния» запустила его повторно. Убиты ВСЕ Creo по имени процесса,
включая свежую рабочую сессию инженера (xtop 14656 + nmsd 9340, старт 11:38:06); несохранённые
правки сессии потеряны, файлы на диске целы.
ПРИЧИНА: факт — разрушительный скрипт не был удалён сразу после исполнения и жил в tmp;
гипотеза — при выборе «чем проверить состояние» рука взяла готовый скрипт по имени, не перечитав
его содержимое (разрушительный вызов был зашит внутри). Нарушены два правила: чистка tmp после
пробы (4.2) и «упавший/разрушительный шаг не повторять» (13.2/ВЕТО).
ПРОФИЛАКТИКА:
1. Разрушительный скрипт (kill/erase/delete/overwrite) удаляется СРАЗУ после первого исполнения;
   в tmp он не живёт ни одной ноги.
2. Перед запуском любого скрипта из tmp — прочитать его содержимое и убедиться, что там нет
   разрушительных вызовов; состояние проверять ОТДЕЛЬНЫМИ read-only пробами.
3. Даже с разрешения пользователя разрушительный вызов требует предупреждения о последствиях
   («умрут ВСЕ инстансы Creo, включая рабочую сессию»); повтор — только новым словом пользователя.
4. В доме `kill_creo` не применять: «мягкий» выход — `stop_creo`; закрытие Creo — рука человека.
5. Шаблон read-only проверки состояния (держать отдельно): `connection:is_creo_running`,
   `creo:pwd`, `file:list`, `Get-Process xtop,nmsd`.
ПОВТОРЫ: 1