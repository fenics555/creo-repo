name: crash_editor-studycheck-halfedit-undefined-name
system: CRASH
description: Use when: файл после правки ссылается на имя, которого нет (NameError), потому что правка оборвана до объявления имени и до прогона проверки
when: halfedit, undefined name, nameerror, skill2, study_check, оборванная правка, полуправка, не объявлено
date: 24.09.2026
executor: Cline
task: культура дома / инструмент study_* (конспект изучения)
ОШИБКА (дословно, для grep):
Traceback (most recent call last):
  File "D:\AI\tools\agent\dev\study_check.py", line 76, in <module>
    fix="сначала read_file по пути, search_kb только по базе знаний", skill=SKILL2)
                                                                            ^^^^^^
NameError: name 'SKILL2' is not defined. Did you mean: 'SKILL'?

СОПУТСТВУЮЩАЯ ОШИБКА (та же нога, другая причина; для grep):
UnicodeEncodeError: 'charmap' codec can't encode character '\u2705' in position 0: character maps to <undefined>

СИМПТОМ: проверка `dev\study_check.py` показывает 20 ✅ и падает на строке про неудачу; файл на диске целый и «почти готов», но в нём имя, которого нет; ход сессии оборвался сразу после правки, поэтому прогон «на потом» не состоялся; пользователь говорит «вылетел».

ПРИЧИНА: факт. Правка добавила в проверку обращения к `SKILL2` (скилл, прикинутый из неудачи), а само объявление `SKILL2 = "проба_неудача"` осталось на следующий вызов — он не случился. Полуправка на диске = заряженный крах: снаружи выглядит сделанной, при запуске падает. Побочная находка: вывод с эмодзи ✅/❌ роняет прогон на консоли cp1251/cp866 (без `-X utf8`).

ПРОФИЛАКТИКА:
1. Правка, вводящая новое имя (переменная, функция, константа) — объявляет его в ТОЙ ЖЕ правке. Оставлять «объявлю следующим вызовом» запрещено: полуправка молчит до первого запуска.
2. Прогон проверки — в том же ходе, где правка, а не «потом»: `py_compile` + сам тест. Незапущенный тест = ложное «сделано».
3. Оборвался — сразу записать: в доме это `study_fail` (раздел «ЧТО НЕ ПОЛУЧИЛОСЬ» в конспекте) + скил здесь, в `crash\`, по конституции.
4. Вывод проверок снимать в UTF-8 внутри Python: `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` в начале проверки (уже стоит в `dev\study_check.py` и `dev\stop_button_check.py`); для полного снятия лога — раннер `D:\AI\log\urn\cline\run_check_capture.py` (пишет вывод в файл без консоли). То же касается ЛЮБОГО своего скрипта с выводом в консоль, включая скрипты-писатели в урне: повтор 24.09 — `culture_tidy2_2026-09-24.py` упал на печати символа `→` уже ПОСЛЕ записи отчёта (работа сделана, вывод умер).
5. Перед объявлением «сделано» сверять цитатой: файл открыть и убедиться, что имена в нём объявлены (grep по имени).

ПОВТОРЫ: 2 (второй — тот же дефект вывода в консоль в скрипте-писателе урны, 24.09.2026; профилактика уже в автозагружаемом слое: `.clinerules`, абзац «КРАШ И ВОСКРЕШЕНИЕ» — строка про ПОЛУПРАВКУ и UTF-8)
