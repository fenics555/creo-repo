name: crash_agent_corruption
executor: local
адаптеры: MANIFEST.md
ПОВТОРЫ: 1
ОШИБКА (дословно, для grep):
SyntaxError: invalid syntax (agent.py при приёме рестарта; живой агент держал старый код, patched файл не импортируется)
СИМПТОМ: агент после рестарта не отвечает или отвечает старым кодом; py_compile правленого файла падает; в худшем случае agent.py повреждён линейным патчем.
ПРОФИЛАКТИКА (из исходного экземпляра): патч-скрипт самопроверяется py_compile + POST /login 200 + дымовой GET, бекап обязателен, при повреждении сначала восстановление из бекапа (data\backup pre_*), линейные патчи agent.py запрещены (спека 66б: только якорные editor-блоки по живым цитатам).
