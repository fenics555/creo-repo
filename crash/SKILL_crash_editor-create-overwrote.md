name: crash_editor-create-overwrote
system: CRASH
description: Use when: вызов editor/write_file в режиме create на СУЩЕСТВУЮЩЕМ файле перезаписал его начисто и убил тело (4256 → 1100 байт)
when: editor, create, overwrite, lost content, усох файл, потерян скилл
date: 23.09.2026
executor: Cline
task: аудит правил 23.09 (восстановление ссылки .clinerules:82)
ОШИБКА (дословно, для grep):
crash_editor-create-overwrote: так потерян скилл карты Давыдовки, 4256 → 1100 байт
СИМПТОМ: файл усох по размеру после «правки»; старое тело заменено одним коротким блоком.
ПРИЧИНА: create-режим/полная перезапись вызвана для уже существующего файла; тело не вошло в новый блок и не проверялось.
ПРОФИЛАКТИКА:
1. Файл есть → только правка якорями (old_text из свежего чтения); полная замена — только осознанным write_file по существующему пути.
2. После create/полной замены — сверка размера до/пост; усох размер без плана = стоп и восстановление (git checkout или копия pre_).
3. Второй «text not found» того же файла = не переписывать, а перечитать и переякориться (см. crash_editor-text-not-found).
ПОВТОРЫ: 1
