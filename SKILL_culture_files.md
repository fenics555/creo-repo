name: culture_files
system: дом
description: Use when: куда положить логи, отчёты, временные файлы и конспект изучения; где правила хранения, сроки и как проверить порядок
when: культура, куда класть, логи, отчёты, урна, временное, конспект, изучение, retention, уборка логов, culture_check
date: 24.09.2026
executor: Cline

# КУЛЬТУРА ФАЙЛОВ ДОМА (куда что кладётся)

**Карта и правило:** `D:\AI\repo\CULTURE_files.md` · **канон:** `.clinerules`, абзац «ОТЧЁТЫ И ЛОГИ»
(копии в корне и в репо обязаны совпадать).
**Проверка порядка:** `python dev\culture_check.py` — 12 проверок, код возврата 1 = есть нарушение.

## Пять адресов
| Что | Куда | Срок |
|---|---|---|
| Логи программ | `D:\AI\log\<имя программы>\` | 56 дн. (agent, harvest, purge), прочим 30 |
| Отчёты | `D:\AI\log\reports\REPORT_<задача>_<исполнитель>_<дата>.md` | 28 дн. |
| Временное и выводы проб | `D:\AI\log\urn\<исполнитель>\` | 14 дн. |
| Память дома (скиллы, спеки, **конспекты**) | `D:\AI\repo\` под гитом | вечно |
| Источники для чтения | `D:\AI\ИЗУЧИТЬ\<тема>\` (**вне гита**) | вечно |

Убирает `log_clean` по `D:\AI\log\retention.json`; старое уходит в `log\_trash_clean\<дата>\` (7 дней, обратимо).
Вне `D:\AI\log` уборщик не трогает ничего. Папки исполнителей в урне создаются сами при первой записи.

## Временное (что именно уходит в урну)
1. скрипт-писатель и патч (`.py .ps1 .cmd .bat`); 2. вывод пробы (`*_run.txt`, `*_err.txt`, `*.json`-снимки);
3. выборка из большого файла (страницы PDF, дамп grep, OCR) — `urn\<исполнитель>\<тема>\`;
4. копия чужого лога для разбора; 5. песочница с фикстурами — `urn\<исполнитель>\sandbox\<метка>\`.
**Не временное:** копия перед правкой (`agent\data\backup\pre_*`), конспект `STUDY_NOTES.md`,
рабочие файлы запускателя (`agent\data\tmp\agent_err.txt`), базы, `repo\`, `ИЗУЧИТЬ\`.

## Изучение (метод «изучил кусок — записал»)
Конспект — `D:\AI\repo\ИЗУЧЕНО\<тема>\STUDY_NOTES.md` (под гитом!). Инструменты агента:
`study_start` → `study_note` (кусок) → **`study_fail` (что не получилось — СРАЗУ)** →
`study_progress` (файл за файлом, папка за папкой) → `study_skill` (заготовки в `repo\BACKLOG_tools.md`).

## Живые находки, из которых родилось правило
- 24.09.2026: уборщик смотрел только подпапки ПЕРВОГО уровня — `urn\<исполнитель>\` (14 дней) и
  `reports\<тема>\` (28) не чистились никогда: 1548 файлов, 255 старше срока. Починено рекурсией + ключом `root`.
- 24.09.2026: `ИЗУЧИТЬ` **вне гита** (`.gitignore` = `/*`) — оттуда исчезли две библиотеки «Новые правила*»
  (нашлись в корзине Windows). Поэтому конспекты переехали в `repo\ИЗУЧЕНО\`.
- 24.09.2026: вывод проверок с эмодзи роняет консоль cp1251 (`UnicodeEncodeError 'charmap'`) —
  в начале проверки ставить `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`.
